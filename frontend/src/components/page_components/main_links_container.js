import { connect } from 'react-redux';
import MainLinks from './main_links';

const mapStateToProps = state => {
    return ({
       
    })
}

const mapDispatchToProps = dispatch => {
    return ({
       
    })
}

export default connect(mapStateToProps, null)(MainLinks)