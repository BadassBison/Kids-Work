import { connect } from 'react-redux'
import ChildIndexBody from './child_index_body'

const mapStateToProps = state => {
    return ({
       
    })
}

const mapDispatchToProps = dispatch => {
    return ({
       
    })
}

export default connect(mapStateToProps, null)(ChildIndexBody)