import { connect } from 'react-redux'
import ParentIndexBody from './parent_index_body'

const mapStateToProps = state => {
    return ({
       
    })
}

const mapDispatchToProps = dispatch => {
    return ({
       
    })
}

export default connect(mapStateToProps, null)(ParentIndexBody)