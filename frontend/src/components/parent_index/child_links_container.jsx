import { connect } from 'react-redux';
import ChildLinks from './main_links';

const mapStateToProps = state => {
    return ({
       
    })
}

const mapDispatchToProps = dispatch => {
    return ({
       
    })
}

export default connect(mapStateToProps, null)(ChildLinks)